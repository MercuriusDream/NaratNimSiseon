import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

The code imports necessary modules, defines a component, and exports it.
```
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```
The code imports necessary modules, defines a component, and exports it.
```
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;
```
The initial code provides the basic structure for a bill list page, but it lacks the actual API call modification for fetching bills using the `ENDPOINTS` constant. Since the provided code snippet doesn't include the API call logic within the `BillList` component or elsewhere in the file, I can only add the import statement for `ENDPOINTS`. To fully address the user's request, the API calls within the `BillList` component (or wherever the calls are made) would need to be modified to use `ENDPOINTS.BILLS`.

```python
import React, { useState } from 'react';
import NavigationHeader from '../components/NavigationHeader';
import HeroSection from '../components/HeroSection';
import BillList from '../components/BillList';
import RecentChanges from '../components/RecentChanges';
import DataMetrics from '../components/DataMetrics';
import Footer from '../components/Footer';
import api from '../api';
import { ENDPOINTS } from '../apiConfig';

const BillListPage = () => {
  const [currentFilter, setCurrentFilter] = useState('all');

  const handleFilterChange = (filter) => {
    setCurrentFilter(filter);
  };

  return (
    <div className="relative pt-20 bg-white">
      <NavigationHeader />
      <main>
        <HeroSection onFilterChange={handleFilterChange} />
        <BillList filter={currentFilter} />
        <RecentChanges />
        <DataMetrics />
      </main>
      <Footer />
    </div>
  );
};

export default BillListPage;